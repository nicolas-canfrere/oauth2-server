{
  "mcpServers": {
    "postgresql": {
      "command": "npx",
      "args": [
        "-y",
        "@executeautomation/database-server",
        "--postgresql",
        "--host", "127.0.0.1",
        "--port", "9876",
        "--database", "oauth2",
        "--user", "admin",
        "--password", "admin"
      ]
    }
  }
}
